#include <stdlib.h>
int indexing(void *L) { return 0; }
const char *world = "world";
enum Tmp { AGENTS, NEXT };
unsigned int w() { return 0; }
// cpp/syntaxes/c.tmLanguage.json
int main() {
	if (true) {
		while (false) {
			switch ((enum Tmp)indexing(NULL)) {
			case AGENTS: // AGENTS:
				break;
			case NEXT: // NEXT:
				break;
			default:
				printf("hello");
			}
		}
	}
	return 0;
}
